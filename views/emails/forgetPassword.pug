doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible', content='IE=edge')
  meta(name='viewport', content='width=device-width, initial-scale=1.0')
  title Welcome to Logcat
  style.
    *{
    margin: 0%;
    padding: 0%;
    box-sizing: border-box;
    }
section(style='width: 100vw; height:100vh; display: flex;justify-content: center;\
align-items: center;')
  section(style='background-color: #27676b;padding: 20px;border-radius: 5px;  ')
    img(src='https://logcat-test.herokuapp.com/static/media/logcarLarge.191493e822543991929f4a742296c16b.svg', width='100px', alt='logo', style='margin-bottom: 10px;')
    h3(style='margin-bottom: 10px;color: #fff;') You have requested to reset your password
    p(style='color: #fff; font-size: 15px; line-height: 24px; margin-bottom: 5px')
      | We cannot simply send you your old password. 
      br
      |  A unique link to reset your
      |                 password has been 
      br
      |  generated for you. To reset your password, 
      br
      | click the
      |                 the button to copy the code.
    button(style='background-color: #fff;border: none; padding: 5px;border-radius: 5px;color: #1F99A4; ', onclick='myFunction()')
    input#myInput(href='https://logcat-test.herokuapp.com/', style='text-decoration:none;color:#1F99A4; border: none; cursor: pointer; text-align: center; outline:none', value={url})
script.
  function myFunction() {
  /* Get the text field */
  var copyText = document.getElementById("myInput");
  /* Select the text field */
  copyText.select();
  copyText.setSelectionRange(0, 99999); /* For mobile devices */
  /* Copy the text inside the text field */
  navigator.clipboard.writeText(copyText.value);
  /* Alert the copied text */
  //   alert("Copied the text: " + copyText.value);
  }
